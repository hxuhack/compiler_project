let a[10]:int;
let b:int = 3 + 4 + 5 * 8 / 2;
let c:int = (1 < 2) && ((5 * 3) < (4 * 4));

fn main()->int{
    let i:int = 0;
    let sum:int = 0;
    let x:int = 200;
    let y:int = 0;

    while(i < 10)
    {
        a[1] = i;
        i = i + 1;
    }

    i = 0;

    if(i < 10)
    {
        sum = sum + a[0];
        i = i * 2;
    }
    ret 0;
}