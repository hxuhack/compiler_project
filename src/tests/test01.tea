fn getch()->int;
fn getint()->int;
fn putint(a:int);
fn putch(a:int);
fn putarray(n:int,a[10]:int);
fn _sysy_starttime(a:int);
fn _sysy_stoptime(a:int);

let a[10]:int;
let b:int = 3 + 4 + 5 * 8 / 2;

fn main()->int{
    _sysy_starttime(4);
    let i:int = 0;
    let sum:int = 0;
    while(i < 10)
    {
        a[i] = i;
        i = i + 1;
    }
    i = 0;
    while(i < 10)
    {
        sum = sum + a[i];
        i = i + 1;
    }
    putint(b);
    putint(sum);
    _sysy_stoptime(19);
    ret 0;
}