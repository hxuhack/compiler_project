fn getch()->int;
fn getint()->int;
fn putint(a:int);
fn putch(a:int);
fn putarray(n:int,a[10]:int);
fn _sysy_starttime(a:int);
fn _sysy_stoptime(a:int);

let a:int = 1;

fn foo(b:int)->int {
    a = b;
    ret 1;
}

fn main()->int {
    _sysy_starttime(9);
    let i:int = 1;
    if((foo(2) > 0) || (foo(3) > 0))
    {
        i = 2;
    }
    putint(a);
    putint(i);
    _sysy_stoptime(17);
    ret 0;
}