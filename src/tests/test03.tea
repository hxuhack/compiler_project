fn getch()->int;
fn getint()->int;
fn putint(a:int);
fn putch(a:int);
fn putarray(n:int,a[10]:int);
fn _sysy_starttime(a:int);
fn _sysy_stoptime(a:int);

let b[10]:int;

fn foo(a[10]:int){
    let i:int = 0;
    while(i < 10) {
        a[i] = getint();
        i = i + 1;
    }
    ret;
}

fn main()->int{
    _sysy_starttime(13);
    let i:int = 0;
    let sum:int = 0;
    foo(b);
    while(i < 10)
    {
        sum = sum + b[i];
        i = i + 1;
    }
    putarray(10,b);
    putint(sum);
    _sysy_stoptime(24);
    ret 0;
}