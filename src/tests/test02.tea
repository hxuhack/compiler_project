fn main(a:int, b:int)->int{
    let a:int;
    let b:int = 0;
    let c:MyStruct;
    ;
    c.node = 1;
    c.len = 3;
    ret foo(c.node, c.len);
}

;

struct MyStruct { 
    node:int,
    len:int  
}

fn foo(a:int, b:int)->int{
    ret a+1+b+2;
}