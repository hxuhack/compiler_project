fn getch()->int;
fn getint()->int;
fn putint(a:int);
fn putch(a:int);
fn putarray(n:int,a[10]:int);
fn _sysy_starttime(a:int);
fn _sysy_stoptime(a:int);

let N:int;

let newline:int;

fn mod(x:int,y:int)->int
{
    ret x-(x/y)*y;
}

fn split(n:int,a[4]:int)->int
{
    let i:int;
    i=N-1;
    while(i!=-1)
    {
        a[i]=mod(n,10);
        n=n/10;
        i=i-1;
    }

    ret 0;
}

fn main()->int
{
    N=4;
    newline=10;
    let i:int;
    let m:int;
    let b[4]:int;
    m = 1478;
    m = split(m,b);
    let t:int;
    i=0;
    while(i<4)
    {
        t=b[i];
        putint(t);
        putch(newline);
        i=i+1;
    }
    ret 0;
}
