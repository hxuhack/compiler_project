fn foo(a2:int, b3:int)->int{
    if(a2 > b3){
        ret b3 - 1;
    }else{
        ret b3 + 1;
    }
}

fn main(a2:int, b3:int)->int{
    while(b > 0){
        c = foo(b, a2);
        c = b - b3;
    }
    
    ret foo(a2, b3);
}

let b:int;
let c:int;


/*
    key:
    type check passed.
*/

