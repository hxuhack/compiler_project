let N:int;
fn insert(a[11]:int,x:int)->int
{
    let p:int;
    let i:int;
    p=0;

    while(x>a[p]&&p<N)
    {
        p=p+1;
    }

    i=N;
    while(i>p)
    {
        let j:int = i - 1;
        a[i]=a[j];
        a[p]=x;
        i=i-1;
    }

    ret 0;
}

fn main()->int
{   
    _sysy_starttime(27);
    N=10;
    let a[11]:int;
    //a[0]=1;
    a[0]=1;
    a[1]=3;
    a[2]=4;
    a[3]=7;
    a[4]=8;
    a[5]=11;
    a[6]=13;
    a[7]=18;
    a[8]=56;
    a[9]=78;
    let x:int;
    let i:int;
    i=0;
    x=getint();
    x=insert(a,x);
    //while()
    while(i<N)
    {
        x = a[i];
        putint(x);
        x= 10;
        putch(x);
        i=i+1;
    }
    _sysy_stoptime(54);
    ret 0;
}